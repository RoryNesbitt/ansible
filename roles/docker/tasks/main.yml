---

- name: Installing docker
  become: true
  package:
    name: docker
    state: present

- name: Create docker group
  become: true
  user:
    name: "{{ ansible_user_id }}"
    groups: docker
    append: true

- name: Start docker service
  become: true
  service:
    name: docker
    state: started
    enabled: true
